"""OptiQ Code — a terminal coding agent for locally-served models.

The best coding agent for *local* models on a Mac: zero-config (it uses whatever
model OptiQ is serving), tuned for operational robustness on weak models, and
co-designed with the OptiQ server for speed. Launched with `optiq code`.

Ported from the retired `conjure/` prototype, stripped of the formal-methods /
verification layer (which was proven to add no benefit). See OPTIQ_CODE_PLAN.md.
"""
